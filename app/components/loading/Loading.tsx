import React from 'react'
import Loader from '../lottie-animations/lottie-loader'

const Loading = () => {
  return (
    <div className='flex justify-center items-center h-screen w-screen'>
      <Loader />
    </div>
  )
}

export default Loading;
